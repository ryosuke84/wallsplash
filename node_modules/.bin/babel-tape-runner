#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel-tape-runner/bin/babel-tape-runner" "$@"
  ret=$?
else 
  node  "$basedir/../babel-tape-runner/bin/babel-tape-runner" "$@"
  ret=$?
fi
exit $ret
